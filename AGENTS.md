# NBA Draft DB エージェント指示

## プロジェクト概要

NBAドラフト指名選手と、NBA公式戦に出場したドラフト外選手を対象に、
ドラフト情報、NBAキャリア、オールスター選出情報を管理・集計する。

MVPでは、リポジトリ内のSQLiteまたはCSVを正本データとし、
そこからLooker Studio用データと静的ページ用データを生成する。

## 基本方針

- 正本データを唯一の入力源とする
- Google Sheets、Looker Studio、静的JSON、静的CSVを手入力で更新しない
- 正本データを更新した後に、すべての出力を再生成する
- データ取得、正規化、検証、集計、公開を分離する
- 同じ入力から同じ出力が得られる再現可能な処理にする

## データルール

- 選手名では結合せず、必ずプロジェクト内のplayer_idを使う
- 外部データソースのIDとplayer_idの対応表を管理する
- すべての外部データにsource_name、source_url、retrieved_atを記録する
- rawデータと正規化済みデータを混在させない
- ドラフト外選手はdraft_status=undraftedとする
- ドラフト外選手のdraft_year、draft_round、overall_pickはNULLとする
- NBA活動シーズン数は、公式戦に出場した異なるシーズン数で計算する
- キャリア期間は初出場シーズンから最終出場シーズンまでの期間として別に保持する
- オールスター選出とオールスター出場を区別する
- selected=trueでもappeared=falseの場合がある

## 変更ルール

- スキーマ変更時は、変更理由と移行方法を記録する
- 集計ロジックを変更した場合は、サンプルデータで結果を確認する
- 外部ソースの仕様を推測で補完せず、出典を確認する
- 大量データを投入する前に、小規模サンプルで処理を検証する
- 生成物だけを修正せず、必ず正本または生成処理を修正する

## ブランチ・Pull Requestルール

- `main`へ直接コミットせず、作業ごとに専用ブランチを作成する
- ブランチ名は原則として`agent/<変更内容>`形式とする
- 変更内容を検証してからコミットし、リモートへPushする
- 実装・調査・文書変更を含む作業は、原則としてPull Requestを作成してレビュー可能な状態にする
- Pull Requestは原則Draftで作成し、対象範囲、変更理由、検証結果を本文に記載する
- 作業ツリーに対象外の変更がある場合、それらを意図せずコミット・Pushしない
- Pull Request作成後も、ローカルIssue文書を判断・合意の正本として更新する

## 検証

最低限、以下を確認する。

- player_idの重複がない
- draft_picksに存在するplayer_idがplayersに存在する
- player_seasonsに存在するplayer_idがplayersに存在する
- overall_pickとdraft_roundが矛盾していない
- games_playedが負数でない
- seasonの形式が統一されている
- selected=falseのオールスター出場データがない
- 集計結果の件数が入力データと大きく乖離していない

## MVPの対象範囲

含めるもの：

- NBAドラフト指名選手
- NBA公式戦に出場したドラフト外選手
- NBAレギュラーシーズン
- 現役選手
- オールスター選出情報

MVPでは原則として以下を含めない：

- ABA・海外リーグの詳細実績
- プレーオフ成績
- 高度な選手評価指標
- 管理画面
- 完全自動の定期スクレイピング

## セッション記録ルール

- セッション内でプロジェクトの目的、要件、設計方針、優先順位、保留事項などの合意が形成された場合は、内容を `docs/Issue/IssueNNN.md` に記録する
- Issue番号は既存の番号を確認し、欠番を意図的に作らず、次の連番を使用する
- Issue文書には、背景、決定事項、対象範囲、完了条件、未決事項を含める
- README.mdには詳細な議論を重複して記載せず、必要に応じてIssue文書やロードマップへのリンクを追加する
- Issue文書を作成・更新した場合は、変更内容と検証結果を作業完了時に報告する

## Skillsの利用方針

- データモデル設計ではnba-data-modelingを使用する
- 外部データ取得ではnba-data-ingestionを使用する
- データ検証ではnba-data-validationを使用する
- 集計処理ではnba-aggregationを使用する
- 静的ページと公開ではnba-publicationを使用する
- CSV、Google Sheets向けデータの確認ではspreadsheets Skillを使用する
- Web表示確認ではplaywright-browser-verify Skillを使用する
- Cloudflare Pages公開ではsites-buildingおよびsites-hostingを使用する

## サブエージェントの責務

- 要件・データモデル担当：スキーマ、指標定義、データポリシー
- データソース・収集担当：外部データ取得とID対応
- データ品質・検証担当：欠損、重複、異常値、整合性
- 集計・Looker Studio担当：集計ロジックと出力データ
- Web公開・運用担当：静的ページ、デプロイ、公開確認

各サブエージェントは、定義済みの正本データとデータポリシーに従う。
独自のスキーマや指標定義を作成してはならない。

## GitHub Issue連携ルール

- セッションで実装・調査・判断事項がIssue化された場合は、まず `docs/Issue/IssueNNN.md` に記録する
- ローカルIssueはGitHub Issueに起票しなくてもよく、ローカルIssue単独で管理を完結できる
- GitHub Issueを起票する場合は、ユーザーが依頼した場合、または作業開始時に起票する方針が明示されている場合に限る
- GitHub Issueを起票する場合は、ローカルIssue文書の内容を要約して登録する
- GitHub Issueのタイトルには、ローカルIssue番号を含める
  - 例：`[Issue001] データモデルと正本データの方針`
- 起票後は、GitHub Issue番号とURLをローカルIssue文書に追記する
- GitHub Issue番号とローカルIssue番号は別物として管理する
- Issue文書の更新とGitHub Issueの更新が必要な場合は、両方を更新する
